<%= form_with(model: comment, local: true) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation" class="alert alert-danger" role="alert">
      <strong><%= pluralize(comment.errors.count, "error") %> prohibited this test from being saved:</strong>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="comment_body">Comment</label>
    <%= f.text_area :body, class: 'form-control', cols: 10, rows: 5 %>
  </div>

  <div class="form-group">
    <%= f.submit t('helpers.submit.submit', model: 'Comment'), class: 'btn btn-primary' %>
  </div>
<% end %>